# 需求分析工作空间说明

这个工作空间用于需求分析、需求梳理、产品方案推演、PRD 产出、用户故事整理，以及基于旧项目经验做迭代判断。

## 工作方式

- 用户通常会直接用自然语言描述需求场景，不一定会提供结构化提示词。
- 接到任务后，先判断当前属于哪一类工作，再决定需要读取哪些目录中的内容。
- 如果信息不足，先向用户提问补充，不要在关键信息缺失时直接输出完整结论。
- 如果用户已经提供了足够材料，再继续往下分析并产出结果。
- 如果工作区里已经存在相关分析文档，优先复用已有上下文，而不是重复创建相同内容。

## 进入工作区后的阅读顺序

1. 先阅读 `.pm-workspace/README.md`
2. 再根据任务需要阅读对应子目录下的 `README.md`
3. 如果任务涉及已有材料，再读取对应的具体文件

不要在一开始就无差别读取整个工作空间，按任务需要逐步读取。

## 目录职责

### `.pm-workspace/`

这是需求分析主工作区，所有过程材料和最终产出优先放在这里。

### `.pm-workspace/inbox/`

用于存放原始输入材料，例如：

- 用户原始想法
- 工单内容
- 会议纪要
- 截图整理
- 聊天记录摘录

默认按项目建子目录存放，例如：

- `.pm-workspace/inbox/kelingsys/`
- `.pm-workspace/inbox/another-project/`
- `.pm-workspace/inbox/general/`

当用户刚抛出一个问题、还没有形成结构化分析时，优先查看对应项目目录。

### `.pm-workspace/references/`

用于存放从旧项目、旧需求、旧 PRD、历史方案中提炼出来的参考资料。

默认按项目建子目录存放，例如：

- `.pm-workspace/references/kelingsys/`
- `.pm-workspace/references/general/`

当任务涉及“参考以前做过什么”“看看旧项目还能怎么迭代”“和之前的方案比较一下”时，优先查看对应项目目录。

如果这里的信息仍然不够，再让用户提供旧项目路径、旧仓库、旧文档，或者让用户把相关材料补充进来。

### `projects/`

这个目录用于挂载需要拿来做深入对比的旧项目。

如果任务需要查看旧项目的真实代码、目录结构、页面实现或历史文件，应明确引导用户把旧项目放到：

- `projects/旧项目名/`

优先建议使用目录联接（junction）方式挂载，而不是复制整个旧项目。

如果用户需要具体操作，应明确告诉用户可以使用：

- `.\mount-legacy-project.ps1 -SourcePath "原项目路径" -AliasName "旧项目名"`

只有在用户明确表示不介意复制副本时，才建议直接复制旧项目到这个目录下。

### `.pm-workspace/analysis/`

用于存放分析过程文件，例如：

- 需求拆解
- 范围判断
- 假设与风险
- 待确认问题
- 旧项目对比分析
- 方案取舍记录

默认按项目建子目录存放，例如：

- `.pm-workspace/analysis/kelingsys/`
- `.pm-workspace/analysis/general/`

凡是“还在思考中”“还需要确认”“属于中间过程”的内容，优先写到对应项目目录。

### `.pm-workspace/outputs/`

用于存放最终版或接近最终版的交付物，例如：

- 需求摘要
- PRD
- 用户故事
- 验收标准
- 路线图建议
- 可发给干系人的正式文档

默认按项目建子目录存放，例如：

- `.pm-workspace/outputs/kelingsys/`
- `.pm-workspace/outputs/general/`

凡是可以直接用于评审、同步、交接的内容，放到对应项目目录。

### `.pm-workspace/scratch/`

用于短期临时材料。默认也按项目建子目录存放，例如：

- `.pm-workspace/scratch/kelingsys/`
- `.pm-workspace/scratch/general/`

这里只适合一次性整理、临时摘录或中途过渡内容，不适合作为长期沉淀目录。

### `.pm-workspace/templates/`

用于存放模板文件。创建新的分析文档或输出文档时，优先参考这里已有模板。

## 其他目录

### `.agents/skills/`

这里是当前工作空间已安装给 Codex 使用的 skills。

不要把这里当作用户的业务材料目录。只有在确实需要确认 skill 内容时，才读取对应 skill 的 `SKILL.md`。

### `tools/Product-Manager-Skills/`

这里存放完整的 `Product-Manager-Skills` 仓库，主要用于：

- 查看原始 skill 内容
- 查看 `commands/`
- 查看文档
- 理解某个方法的完整上下文

如果任务需要查看命令工作流、原始说明或更多参考材料，可以去这个目录里读取对应文件。

### `pm-analysis.code-workspace`

这是推荐在 VS Code 中打开的工作区文件。它把需求分析目录和技能仓库放在同一个视图中。

## 面对不同任务时的处理方式

### 场景一：已经有 PRD，想知道怎么落地

- 先检查用户是否已经提供 PRD 文件路径或内容
- 如果有现成 PRD，先判断它是否已经具备落地所需的关键信息
- 如果缺少范围、依赖、验收标准、里程碑、目标用户或成功标准，先指出缺口并提问
- 分析过程写入 `.pm-workspace/analysis/项目名/`
- 可以直接执行的结果输出到 `.pm-workspace/outputs/项目名/`

### 场景二：只有一个想法，想完善成可以落地的 PRD

- 不要默认立刻开始写 PRD
- 先确认问题、目标用户、业务目标、边界、风险和需要验证的假设
- 如果想法还很模糊，先进入需求澄清和发现阶段
- 中间分析写到 `.pm-workspace/analysis/项目名/`
- 当信息足够稳定后，再整理成 `.pm-workspace/outputs/项目名/` 下的正式 PRD

### 场景三：有一个旧项目和一个新想法，想判断旧项目下一步应该做什么迭代

- 先看 `.pm-workspace/references/旧项目名/` 中是否已有旧项目摘要
- 如果没有摘要，不要默认卡住
- 如果旧项目已经挂载到 `projects/旧项目名/`，可以先基于旧项目内容生成一份摘要，再继续分析
- 如果既没有摘要，也没有挂载的旧项目，再让用户补充旧项目背景或提供旧项目路径
- 先判断旧项目当前阶段、已有能力、核心问题和限制，再评估新想法是否值得做
- 如果需要代码级对比或实现级确认，不要只说“提供旧项目”，要明确告诉用户把旧项目放到 `projects/旧项目名/`
- 当说明旧项目接入方式时，优先建议使用目录联接，不要默认建议复制整个项目
- 如果当前工作区里还没有旧项目目录，要明确告诉用户当前无法直接查看旧项目实现
- 在这种情况下，要给出明确步骤，例如：
  1. 如果已有旧项目代码，先把旧项目以目录联接方式挂载到 `projects/旧项目名/`
  2. 再基于旧项目生成摘要，并写入 `.pm-workspace/references/旧项目名/`
  3. 然后继续做对比分析、迭代判断和建议输出
- 中间判断和比较结果写入 `.pm-workspace/analysis/项目名/`
- 明确的迭代建议、优先级和下一步建议写入 `.pm-workspace/outputs/项目名/`

## 提问要求

- 当信息不足时，先提问
- 优先问最影响判断的问题
- 问题应简洁、具体、能帮助推进下一步
- 避免在信息不足时输出过度完整但其实建立在猜测上的结果

## 文件使用原则

- 新建文件前，先看是否已经存在同主题文件
- 如果已有分析还可以继续沿用，优先续写而不是重复新建
- 文件名尽量体现主题和用途
- 同一项目的材料优先放到对应项目子目录中，例如 `analysis/项目名/`
- 最终交付物放 `outputs/项目名/`
- 原始输入放 `inbox/项目名/`
- 历史参考放 `references/项目名/`
- 如果当前还没有明确项目名，临时放到 `general/`，后续再归档

## 关于 skills

- 这个工作空间已经安装了一批和需求分析相关的 Codex skills
- 是否使用某个 skill，由 AI 根据用户请求和已安装 skill 的说明自行判断
- 不要在没有必要时人为强行指定 skill 组合
- 如果用户明确指定某个 skill 或某个命令工作流，再按用户要求执行

## 关于旧项目

- 旧项目材料优先做摘要后放入 `references/旧项目名/`
- 如果已有挂载的旧项目但还没有摘要，可以先从旧项目生成摘要
- 只有在摘要不足以支持判断时，才进一步查看旧项目原始内容
- 当需要查看旧项目原始内容时，明确引导用户把旧项目以目录联接方式挂载到 `projects/旧项目名/`
- 如果当前工作区没有旧项目内容，不要假装已经看过
- 当旧项目未挂载时，要明确说明“目前只能基于摘要分析，无法基于旧项目实现分析”
- 当需要更多历史上下文时，明确告诉用户还缺什么
